imports: null

constants:

string_constants: null

aliases:
  TIMESTAMP_TYPE: uint32

host_ids: null

module_ids:
  CONFIG_MODULE: 13

struct_defs:

message_defs:
  ALIAS_TEST:
    id: 6000
    fields:
      timestamp: TIMESTAMP_TYPE
      times: TIMESTAMP_TYPE[32]

  TEST_START:
    id: 6001
    fields:
      id: int32

  TEST_END:
    id: 6002
    fields:
      id: int32

  TEST_METADATA:
    id: 1904
    fields:
      id: int32
      note: char[128]